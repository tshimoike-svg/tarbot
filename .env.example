# J-Quants 認証情報（このファイルをコピーして .env を作る。 .env は git 管理外）
#   cp .env.example .env  して値を埋める
# 方式1：メール＋パスワード（推奨。クライアントが自動でトークンを発行・更新）
JQUANTS_MAILADDRESS=
JQUANTS_PASSWORD=
# 方式2：リフレッシュトークンを直接指定する場合（上の2つは不要）
# JQUANTS_REFRESH_TOKEN=

# 発注フラグ（既定はドライラン。本番発注は人間が明示的に解除する）
# DRY_RUN=false
